import 'package:flutter/material.dart';

class UnKnownRoute extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("UnKnown"),
      ),
      body: Center(child: Text("UnKnoen route page")),
    );
  }
}
